.row.user_dett
	.col-md-9
		.media
			.pull-left
				img.media-object(alt=__("Thumbnail of")+": "+result.display_name, src=Fnc.getFileFormat(result.files, 240, 180))
			.media-body
				h1.media-heading=result.display_name
				.cnt
					ul.user_dett_ul
						- if (result.websites)
							- for(a=0;a<result.websites.length;a++)
								li
									a(href=result.websites[a].url,target="_blank")
										i.glyphicon.glyphicon-globe
										!=result.websites[a].url
						- if (result.locations)
							li
								i.glyphicon.glyphicon-home
								!=Fnc.formatLoc(result.locations)
		.row
			- for(a=0;a<result.length;a++)
				.col-md-3
					.row
						.col-md-1

	.col-md-3
		ul.user_dett_ul.pull-right.text-right
			li
				a.btn.btn-mini
					i.glyphicon.glyphicon-globe
					!=" "+__("Download PDF")
				!=" "
			li
				a.btn.btn-mini.btn-primary
					i.glyphicon.glyphicon-globe.glyphicon.glyphicon-white
					!=" "+__("Share")
			li
				a.btn.btn-mini.btn-primary
					i.glyphicon.glyphicon-globe.glyphicon.glyphicon-white
					!=" "+__("Invite to event")
			li
				a.btn.btn-mini
					i.glyphicon.glyphicon-globe
					!=" "+__("Messagge")
			li
				a.btn.btn-mini.btn-primary
					i.glyphicon.glyphicon-globe.glyphicon.glyphicon-white
					!=" "+__("Follow")
hr
ul.nav.nav-tabs
	li(class=!sez ? "active" : undefined)
		a(href="#about",onclick="$('#about').show();")=__("About")
	- for(item in result.stats)
		- if(_config.sections[item] && result.stats[item])
			li
				a(href="/"+result.permalink+"/"+_config.sections[item].basepath+"/")=_config.sections[item].title+" ["+result.stats[item]+"]"
